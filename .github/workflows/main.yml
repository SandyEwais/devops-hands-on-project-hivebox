name: testing
on:
  pull_request: 
    branches:
      - main
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v7
      - name: run tests
        run: bash test.sh